# Phase 3 Precision Lookup Route Implementation Plan

1. Add route types for exact page/section precision lookup.
2. Add failing tests for service, operation, benchmark shape, and CLI help.
3. Implement a read-only service that resolves exact manifest or section rows.
4. Expose `precision-lookup-route` through the shared operation surface.
5. Add a benchmark script and verification runbook entry.
6. Run targeted tests, benchmark, then shared Phase 2 and Phase 1 regressions.
