# Phase 3 Retrieval Route Selector Implementation Plan

1. Add selector types for explicit intent dispatch.
2. Add failing tests for service, operation, benchmark shape, and CLI help.
3. Implement a small selector service over the existing route primitives.
4. Expose `retrieval-route` through the shared operation surface.
5. Add benchmark and verification coverage.
6. Run targeted tests, benchmark, then shared Phase 2 and Phase 1 regressions.
