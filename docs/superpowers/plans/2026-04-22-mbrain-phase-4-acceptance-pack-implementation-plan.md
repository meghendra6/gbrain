# Phase 4 Acceptance Pack Implementation Plan

1. Add a failing benchmark-shape test for `phase4-acceptance-pack`.
2. Add an aggregator script over the published Phase 4 benchmark slices, including personal-episode lookup and personal-write-target once they are published.
3. Publish `bench:phase4-acceptance` and include the new test in `test:phase4`.
4. Document verification commands in `docs/MBRAIN_VERIFY.md`.
5. Run the pack test, the pack benchmark, and `test:phase4`.
